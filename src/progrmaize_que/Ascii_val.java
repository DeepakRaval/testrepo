package progrmaize_que;

public class Ascii_val {
	
	public static void main (String[]args){
		char latter = 'a';
		int ascii  = latter;
		int ascii_1  = (int) latter; // thus v can explicit to int.
		
		// ASCII is the special value of character, symbols and number.
		
		System.out.println(ascii);
		System.out.println(ascii_1);
	}

}
